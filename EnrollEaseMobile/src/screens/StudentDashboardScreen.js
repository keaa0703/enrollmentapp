android {
    compileSdkVersion 33 // Use the latest SDK version
    defaultConfig {
        applicationId "com.example.enrolleasemobile"
        minSdkVersion 21 // Minimum SDK version
        targetSdkVersion 33 // Target SDK version
        versionCode 1 // Increment this for each release
        versionName "1.0" // Update this for each release
    }
}